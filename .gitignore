__pycache__/
*.pyc
*.egg-info/
*.egg-info
*.log
.env
.venv/
venv/
*.whl
# build artifacts — never commit wheels/sdists
# GUARDRAIL: dist/ and build/ are pip/build outputs; committing them bloats the
# repo and can shadow fresh builds during parity testing.
dist/
build/
# one-off pip bootstrap installer — never commit it again
# GUARDRAIL: get-pip.py was left in the repo because the venv had no pip;
# bootstrap with it, then delete — it bloats every self-dump by 2.2MB
get-pip.py
.pytest_cache/
.coverage
htmlcov/
